import React from "react";
import CityForm from "../components/CityForm";

const AddCity = () => {
    return (
        <div>
            <h1>Add City</h1>
            <CityForm />
        </div>
    );
};

export default AddCity;
